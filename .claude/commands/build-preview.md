Build the project and preview the production output.

Steps:
1. Run `npm run build` (runs tsc -b && vite build)
2. Report build result (success/failure, bundle size)
3. If build succeeds, run `npm run preview` to start preview server
4. Report the preview URL
