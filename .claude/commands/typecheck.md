Run full TypeScript type check on the project.

Steps:
1. Run `npx tsc -b` and capture output
2. If errors exist, list each error with file path and line number
3. Suggest fixes for each error
4. After fixing, re-run type check to verify
