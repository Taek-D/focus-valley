Run ESLint and auto-fix issues.

Steps:
1. Run `npm run lint` to see current issues
2. If there are auto-fixable issues, run `npx eslint . --fix`
3. Re-run lint to verify remaining issues
4. For non-auto-fixable issues, suggest manual fixes
